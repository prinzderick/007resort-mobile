import 'package:flutter/material.dart';

/// Placeholder: Outlet supervisor view (Restaurant, Indoor Club, Pool Bar, Bush Bar/Event Centre).
///
/// Phase 0 scaffolding only - no business logic lives in the client.
class SupervisorScreen extends StatelessWidget {
  const SupervisorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Supervisor')),
      body: const Center(child: Text('Supervisor - coming soon')),
    );
  }
}
