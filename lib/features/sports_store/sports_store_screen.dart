import 'package:flutter/material.dart';

/// Placeholder: Scan QR -> show purchased/rented entitlement from the API -> record release/return via the API.
///
/// Phase 0 scaffolding only - no business logic lives in the client.
class SportsStoreScreen extends StatelessWidget {
  const SportsStoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sports store')),
      body: const Center(child: Text('Sports store - coming soon')),
    );
  }
}
