import 'package:flutter/material.dart';

/// Placeholder: Reception checks a shared attendant tablet out to a staff member for a shift (tablet -> staff -> shift -> facility). Recorded by the API.
///
/// Phase 0 scaffolding only - no business logic lives in the client.
class DeviceCheckoutScreen extends StatelessWidget {
  const DeviceCheckoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Device checkout')),
      body: const Center(child: Text('Device checkout - coming soon')),
    );
  }
}
